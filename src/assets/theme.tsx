import { createTheme } from '@mui/material';
import React from 'react';

export const fedExTheme = createTheme({
    palette: {
        primary: {
            main: "#4d148c"
        },
        text: {
            primary: "#FFFFFF",
            secondary: "#333333"
        }
    }
})